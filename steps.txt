sudo apt-get update

sudo apt-get install linux-image-generic
sudo apt-get install curl

# Get the latest Docker package
curl -sSL https://get.docker.com/ | sh

# Create the docker group and add your user.
sudo usermod -aG docker arthur


# Install Sickbeard
mkdir /home/arthur/mediacenter
docker run -d --name="sickrage" -v /home/arthur/mediacenter/sickrage/config:/config -v /home/arthur/mediacenter/downloads:/downloads -v /home/arthur/mediacenter/tv:/tv -v /etc/localtime:/etc/localtime:ro -p 8081:8081 needo/sickrage
